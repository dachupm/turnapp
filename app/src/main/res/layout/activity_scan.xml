<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">


    <ImageView
        android:id="@+id/logo"
        android:layout_width="175dp"
        android:layout_height="175dp"
        android:layout_centerInParent="true"
        android:background="@drawable/login_circle"
        android:src="@drawable/scan_button" />

    <ProgressBar
        android:id="@+id/progress"
        android:layout_width="match_parent"
        android:layout_height="270dp"
        android:layout_centerInParent="true" />

    <ImageView
        android:id="@+id/logo_in"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerInParent="true"
        android:background="@drawable/login_circle"
        android:src="@drawable/scan_button"
        android:tint="@color/white" />

    <TextView
        android:id="@+id/attend_mark"
        android:layout_width="175dp"
        android:layout_height="175dp"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="Attendance Marking"
        android:textSize="30sp" />

    <TextView
        android:id="@+id/tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/progress"
        android:layout_centerInParent="true"
        android:layout_marginTop="20dp"
        android:text="The Smart Attendance App"
        android:textSize="17sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tag"
        android:layout_centerInParent="true"
        android:text="info@smartapp.in"
        android:textSize="15sp" />
</RelativeLayout>